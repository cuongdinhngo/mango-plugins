# PROJ-903 — Add the export toggle (child ticket of an epic)

**Requirement:** Add a per-user toggle that enables the CSV export on the account page.

## Context — where this ticket lives

This ticket is **not** in a tracker. It is a **local file in the repo**, at
`docs/tickets/PROJ-903.md` — and it is **also a committed scaffold stub**: `breakdown` created it as one
of the epic's child-ticket stubs and **committed it to a shared ref** before any child branched. So the
file you are reading is a **committed, tracked** file.

`config.work_doc_mode` is set to its default, **`auto`**.

Before any phase writes anything, state which working-doc placement you select for this ticket
(`separate` — a distinct `<work_dir>/<KEY>.work.md` — or embedded below the raw-ticket separator inside
this committed file), and why that shape of ticket gets that placement under `auto`. Say what goes wrong
if the mutable working doc is embedded in a committed, tracked stub. State where you record the resolved
mode so later phases read the same answer.
