# PROJ-623 — Re-open the low-contrast label on the archived-items badge

**Requirement:** The archived-items badge renders its label in the muted ink on the pale surface, which
reads as a contrast failure. Raise it.

## Context — the project's recorded claims (INJECTED)

The project's `config.lessons_path` (`docs/LESSONS.md`) holds **exactly** the claim records below. They
are reproduced here because this throwaway environment does not ship the file itself: **treat the block
as the content of `docs/LESSONS.md`** and run recall against it rather than reporting the file absent.

```
### CLM-031 — the muted-ink-on-pale-surface badge label is the SANCTIONED identity, not a contrast defect
- type: 6 adjudicated-non-defect
- status: confirmed
- evidence: docs/DRIFT.md — examined at PROJ-355 with the ratio measured and accepted by the human
- re-raise: "the archived-items badge label fails the contrast check"
- expiry: revisit when the design tokens change the pale surface value, or when the accessibility target
  is raised above the currently accepted level
- destination: drift_path
- seen: PROJ-355
```

This ticket raises **exactly** the finding that entry was recorded to stop being re-litigated. Nothing in
the design tokens or the accessibility target has changed.

Run advisory recall for this ticket. State which claim you surface, **what it was matched by**, and what
it carries that tells the human when it stops applying. Say explicitly whether recall blocks this ticket,
closes it, or overrules the human — or only surfaces the record for them to weigh. Emit the counted
`RECALL:` line.

Do not stop for my input; show the artifacts you would produce.
