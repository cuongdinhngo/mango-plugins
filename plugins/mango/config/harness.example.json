{
  "//": "Copy this file to your project repo root as .harness.json and fill in the values. It is the per-project contract mango reads at runtime. SECRETS/TOKENS DO NOT BELONG HERE: keep them in a gitignored .env. .harness.json itself is gitignored by mango's marketplace, but treat it as committed project config — never paste credentials into it.",

  "//rulebook_path": "May be a FILE or a DIRECTORY. If a directory, every consumer (reviewer/onboarder agents, grounding skills) reads all *.md inside it. Default to one file so the whole rule set always loads; point at a folder to split later.",
  "rulebook_path": "docs/ENGINEERING_RULES.md",
  "standards_path": "docs/CODING_STANDARDS.md",

  "repos": [
    { "name": "app", "root": "." },
    { "name": "shared-lib", "root": "../shared-lib" }
  ],

  "test_command": "REPLACE_ME (e.g. the single command that runs your test suite or a focused subset)",

  "tickets_dir": "docs/tickets",
  "//work_dir": "Directory for mango's per-ticket WORKING DOC (<KEY>.work.md) — the mutable state doc carrying all five phases. Kept SEPARATE from the ticket spec so the challenger's input can exclude it. Defaults to tickets_dir if unset.",
  "work_dir": "docs/tickets",
  "//work_doc_mode": "Where the working doc lives: auto | separate | embed (default auto). A tracker-hosted ticket always gets a separate <work_dir>/<KEY>.work.md. When the ticket is itself a local file in the repo, auto/embed APPEND the working doc to that file below a raw-ticket separator line (one file, no duplicate); separate always writes a distinct file. The raw ticket portion stays above the separator either way, so the challenger payload can exclude the working doc. GUIDANCE: for a local-file ticket that is ALSO a committed scaffold stub (e.g. an epic child-ticket stub committed by breakdown), prefer separate — embedding the mutable working doc in a committed, tracked file leaves its edits as uncommitted changes to a TRACKED file, which is fragile to any stray subagent git-state op; a distinct <KEY>.work.md avoids that fragility.",
  "work_doc_mode": "auto",
  "//pr_checklist_path": "OPTIONAL path to a project-owned finalise checklist (e.g. a PR-template or definition-of-done file). If set, finalise walks each item and reports satisfied/not-satisfied/N-A with evidence before drafting the PR, surfacing any unmet item at the final gate. mango supplies the mechanism; the project supplies the content. Omit or null to disable.",
  "pr_checklist_path": null,
  "//stuck_threshold": "Circuit-breaker: after this many failed attempts against the SAME proving artifact / failing-test signature, execute and quick STOP and escalate to the user instead of retrying. The counter resets when the failing signature changes. Default 3.",
  "stuck_threshold": 3,
  "explore_fanout": true,

  "//track": "Which gate set(s) apply: backend | frontend | fullstack (default backend). ORTHOGONAL to TIER (TIER = process weight; track = which gates). backend behaves exactly as pre-0.7. frontend adds the falsifiable a11y/token rubric + the M1–M10 responsive/touch gates. fullstack applies both gate sets. When unset, mango may INFER the track from touched-file paths (k/N files under UI paths). A ticket may be track=frontend + TIER=lite.",
  "track": "backend",
  "//breakpoints": "OPTIONAL list of viewport widths (CSS px) the frontend responsive gates test against, e.g. [375, 768, 1280]. The 320 px reflow floor is a STANDARD, always tested when track includes frontend regardless of this list. When a declared width is a small viewport, the width-parametric gates (M2 no-h-scroll, M3 reflow) come into scope. Ignored on the backend track.",
  "breakpoints": null,
  "//design_doc_path": "OPTIONAL path to the per-project DESIGN.md contract (default DESIGN.md at repo root). On the frontend track, design creates/updates it (palette/typography/spacing tokens, shell vs data-core split, the Responsive & touch choices) and the review rubric is scored AGAINST it. Breakpoint values, navigation pattern, and which regions collapse vs reflow live here — they are project CHOICES, never gated by mango.",
  "design_doc_path": "DESIGN.md",
  "//cost_tier": "Model-delegation dial: economy | standard | max (default standard). economy pushes more bulk retrieval to a Haiku worker and avoids Opus on review; max allows Opus on review for high-stakes diffs. Never demotes review/challenger to Haiku. See PRINCIPLES.md routing map.",
  "cost_tier": "standard",

  "//token_optimizer": "Human-gated record (via /mango:budget) of which external token optimizers mango may assume are present. DESCRIPTIVE + human-gated, exactly like codify: mango DETECTS + INFORMS + records a human's choice — it NEVER installs an optimizer, never DEPENDS on one, and never lets one weaken a check/gate/critic. Safety axis: an optimizer is safe only if it removes representation redundancy (how output is phrased), never a check, a gate, a critic, or the evidence detail a critic relies on (path:line, measured values, diffs). rtk: expect|off (default expect) — mango tolerates RTK rewriting Bash-command output into a compact form; it does not install RTK and DEGRADES CLEANLY when RTK is absent (nothing fails, no decision changes — only the saving is lost). headroom.output_shaper MUST stay false (it changes what the model writes / how hard it thinks → can weaken a critic). caveman.scope is HARD-PINNED to non-critic-only: Caveman-style output compression must NEVER be applied to reviewer/challenger/gate-blocking output, which must retain full evidence detail. Enabling any optimizer is a recorded provisional decision (ratified like codify), never a silent toggle.",
  "token_optimizer": {
    "rtk": "expect",
    "headroom": { "enabled": false, "output_shaper": false },
    "caveman": { "enabled": false, "scope": "non-critic-only" }
  },
  "//update_check_url": "OPTIONAL raw URL to the published marketplace manifest (e.g. the raw marketplace.json of the marketplace mango is published from). If set, /mango:version-check fetches it to compare the running version against the latest published version and PRINTS the host /plugin commands when a newer one exists. mango never updates or installs — plugin administration is the host's job. Leave unset/null to disable the check (no network call).",
  "update_check_url": null,
  "branch_strategy": "fix|feat|chore/<KEY>-<slug>",
  "lessons_path": "docs/LESSONS.md",
  "pr_host": "REPLACE_ME (e.g. the host/command used to open a pull request)",

  "//docs_dir": "OPTIONAL directory where the opt-in descriptive adapters write their regenerable maps (sitemap.md, db-map.md). Descriptive facts only — never normative rules. Unset → the adapters have nowhere to write and report they are not configured.",
  "docs_dir": null,
  "//code_map_cmd": "OPTIONAL command for /mango:sitemap to emit the code surface (routes/endpoints + modules). Opt-in & stack-specific; unset → sitemap reports it is not configured and does nothing.",
  "code_map_cmd": null,
  "//db_kind": "OPTIONAL database kind (generic label, e.g. relational/document). Required, with one of db_introspect_cmd or migrations_path, to enable /mango:db-map. db-map is OFF by default.",
  "db_kind": null,
  "//db_introspect_cmd": "OPTIONAL read-only introspection command for /mango:db-map to derive the schema map. Must never alter or migrate the schema. Use this OR migrations_path.",
  "db_introspect_cmd": null,
  "//migrations_path": "OPTIONAL directory of migration files for /mango:db-map to derive the schema from when no live introspection command is available. Use this OR db_introspect_cmd.",
  "migrations_path": null,

  "tracker": {
    "base_url": "https://tracker.example.com",
    "project_key": "PROJ",
    "cli": "REPLACE_ME (CLI command for ALL tracker writes — comments, transitions; never an MCP)",
    "read_mcp": "REPLACE_ME_OR_NULL (optional read-only MCP server name for ticket READS only)",
    "//fields": "OPTIONAL list of fields to request on a ticket READ, so one read returns the full ticket (some trackers default to a minimal set and return an empty description). If unset, analysis requests a sensible full default (description/body, type, labels, parent, priority).",
    "fields": ["description", "type", "labels", "parent", "priority"]
  },

  "cause_taxonomy": [
    "logic",
    "data",
    "config",
    "integration",
    "concurrency",
    "validation",
    "ui"
  ],

  "ticket_header_schema": {
    "Constraint": "C",
    "Requirement": "R",
    "Goal": "G",
    "Acceptance Criteria": "AC"
  }
}
